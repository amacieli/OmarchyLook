# Optional: omarchy-look configuration
# Note: Azure App Registration is NOT required for end users.
# The app uses Microsoft's public client ID and Device Flow authentication.

# Optional: any future configuration goes here
# (currently empty — the app works out of the box)
